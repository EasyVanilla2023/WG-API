# WireGuard Installation Script

Автоматический скрипт для установки и настройки WireGuard VPN сервера через Docker.

## 🚀 Быстрый старт

### Требования

- Ubuntu/Debian сервер с root доступом
- IP адрес сервера (публичный)
- Минимум 1ГБ RAM

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/EasyVanilla2023/wg-install-sh.git
cd wg-install-sh
```

2. Настройте переменные окружения:
```bash
cp env.example .env
nano .env  # или используйте любой редактор
```

3. Запустите скрипт:
```bash
chmod +x setup-wireguard.sh
source .env
./setup-wireguard.sh
```

### Использование с автоматическим созданием первого клиента

```bash
export WG_HOST=your_server_ip
export AUTH_TOKEN=your_secure_token
export CREATE_FIRST_CLIENT=true
./setup-wireguard.sh
```

## 📋 Переменные окружения

| Переменная | Описание | Обязательная |
|-----------|----------|--------------|
| `WG_HOST` | IP адрес вашего сервера | Да |
| `AUTH_TOKEN` | Токен для авторизации в API | Да |
| `CREATE_FIRST_CLIENT` | Автоматически создать первого клиента | Нет |

## 🔧 Использование API

После установки WireGuard API будет доступен на порту `3000`.

### Создание клиента

```bash
curl -X POST \
  -H "Authorization: Bearer $AUTH_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"data": {"user_id": 123, "comment": "client name"}}' \
  http://your_server_ip:3000/api/clients
```

### Получение конфигурации клиента

```bash
curl -H "Authorization: Bearer $AUTH_TOKEN" \
  http://your_server_ip:3000/api/clients/1 \
  --output client1.conf
```

### Получение QR кода

```bash
curl -H "Authorization: Bearer $AUTH_TOKEN" \
  http://your_server_ip:3000/api/clients/1?format=qr \
  --output client1.png
```

## 📝 Полезные команды

### Проверка статуса

```bash
docker ps | grep wg-rest-api
docker logs wg-rest-api
```

### Остановка/Запуск

```bash
docker stop wg-rest-api
docker start wg-rest-api
```

### Перезапуск

```bash
docker restart wg-rest-api
```

## 🔒 Безопасность

- Используйте сильный `AUTH_TOKEN` (минимум 32 символа)
- Настройте файрвол для ограничения доступа к порту 3000
- Регулярно обновляйте Docker образ

## 🛠 Устранение неполадок

### Docker не запускается

```bash
systemctl status docker
systemctl start docker
systemctl enable docker
```

### Контейнер не запускается

```bash
docker logs wg-rest-api
docker ps -a | grep wg-rest-api
```

### API не отвечает

Проверьте, что:
- Контейнер запущен: `docker ps | grep wg-rest-api`
- Порт 3000 открыт: `netstat -tuln | grep 3000`
- Используете правильный токен: `echo $AUTH_TOKEN`

## ⚠️ Примечание

Этот скрипт предназначен для использования на чистом Ubuntu/Debian сервере. На серверах с уже установленным Docker могут возникнуть конфликты.

