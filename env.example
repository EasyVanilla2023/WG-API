# Пример файла с переменными окружения
# Скопируйте этот файл и заполните своими данными
# Использование: source env.example && ./setup-wireguard.sh

export WG_HOST=IP адрес НОДЫ
export AUTH_TOKEN=ПАРОЛЬ API (Придумать)

# Опционально: автоматически создать первого клиента после установки
# export CREATE_FIRST_CLIENT=true

# Опционально: использовать GitHub Container Registry вместо Docker Hub
# Это поможет обойти лимиты Docker Hub (100 pull в 6 часов для анонимных пользователей)
# export DOCKER_IMAGE=ghcr.io/username/wg-rest-api

